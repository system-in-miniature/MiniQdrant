failure_files = ["tests/index/test_hnsw_graph.py", "tests/index/test_hnsw_recall.py", "tests/index/test_hnsw_search.py"]

[[blocks]]
id = "mechanism"
title_en = "Deterministic HNSW search mechanism"
title_zh = "确定性 HNSW 搜索机制"
summary_en = "graph construction and search are reproducible and never return deleted, duplicate, or out-of-scope candidates."
summary_zh = "Graph 构建与搜索可复现，绝不返回已删除、重复或越界 Candidate。"
files = ["src/miniqdrant/index/hnsw.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/miniqdrant/index/__init__.py"]
supporting = true
