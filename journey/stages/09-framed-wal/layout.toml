failure_files = ["tests/reliability/test_wal_replay.py", "tests/reliability/test_wal_tail.py", "tests/storage/test_wal_codec.py"]

[[blocks]]
id = "mechanism"
title_en = "Framed write-ahead logging mechanism"
title_zh = "分帧预写日志机制"
summary_en = "a mutation is publishable only after its complete frame is durable, and recovery never skips corruption to invent later history."
summary_zh = "Mutation 只有完整 Frame 持久后才能发布，恢复绝不跳过损坏去虚构后续历史。"
files = ["src/miniqdrant/persistence/frame.py", "src/miniqdrant/persistence/fsync.py", "src/miniqdrant/persistence/wal.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/miniqdrant/persistence/__init__.py"]
supporting = true
