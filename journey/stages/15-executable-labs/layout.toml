failure_files = ["tests/acceptance/test_labs.py"]

[[blocks]]
id = "mechanism"
title_en = "Executable vector-search labs mechanism"
title_zh = "可执行向量搜索实验机制"
summary_en = "each fresh-process lab exposes one mechanism through stable observable output without private fixtures or pre-existing state."
summary_zh = "每个新进程 Lab 都通过稳定可观察输出展示一个机制，不依赖私有 Fixture 或既有状态。"
files = ["src/miniqdrant/index/hnsw.py", "src/miniqdrant/labs/filtering.py", "src/miniqdrant/labs/plan_comparison.py", "src/miniqdrant/labs/recall.py", "src/miniqdrant/labs/recovery.py", "src/miniqdrant/labs/segments.py", "src/miniqdrant/persistence/frame.py", "src/miniqdrant/persistence/manifest.py", "src/miniqdrant/persistence/wal.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["pyproject.toml"]
supporting = true
