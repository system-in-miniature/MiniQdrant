failure_files = ["tests/index/test_quantization.py", "tests/query/test_quantized_rescore.py"]

[[blocks]]
id = "mechanism"
title_en = "Quantized candidate rescoring mechanism"
title_zh = "量化候选精排机制"
summary_en = "quantization may change candidate cost but returned scores and ordering come from exact vectors after rescoring."
summary_zh = "Quantization 可以改变候选成本，但返回分数与顺序来自精确 Vector 的再次评分。"
files = ["src/miniqdrant/index/quantization.py", "src/miniqdrant/query/executor.py", "src/miniqdrant/segment/immutable.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/miniqdrant/index/__init__.py"]
supporting = true
