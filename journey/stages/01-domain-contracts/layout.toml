failure_files = ["tests/test_project_contract.py", "tests/unit/test_domain.py"]

[[blocks]]
id = "mechanism"
title_en = "Domain contracts mechanism"
title_zh = "领域契约机制"
summary_en = "accepted values are owned and canonical, dimensions remain fixed, and unsupported types fail at the public boundary."
summary_zh = "被接受的值由系统拥有且具有规范表示，维度保持固定，不支持的类型在公共边界失败。"
files = ["src/miniqdrant/config.py", "src/miniqdrant/errors.py", "src/miniqdrant/ids.py", "src/miniqdrant/json_values.py", "src/miniqdrant/models.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["pyproject.toml", "src/miniqdrant/__init__.py", "uv.lock"]
supporting = true
